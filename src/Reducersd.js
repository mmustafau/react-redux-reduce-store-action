// ayrı 2 reducer yapılabilir.

var reduxx = "";
/*

export default function reducerd(state = {
    count2:0

},action) {

    if (action.type ==="INCREMENTDOUBLE"){

        return {
            ...state,
            count2: state.count2 +2
        }

    }

    else if (action.type ==="DECREMENTDOUBLE"){
        return {
            ...state,
            count2: state.count2 -2
        }

    }

    return state;

}*/